{
  "name": "web-check-api",
  "version": "1.0.0",
  "description": "OSINT API for analysing any website - equivalent of web-check",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "jest"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "jest": "^30.3.0",
    "supertest": "^7.2.2"
  },
  "dependencies": {
    "dotenv": "^17.4.2"
  }
}
